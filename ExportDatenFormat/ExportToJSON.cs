/*using Newtonsoft.Json;

namespace ExportDatenFormat
{
    internal class ExportToJSON
    {
        private void ExportToJson(string fileName)
        {
            // Serialize the dataList to a JSON string
            string jsonData = JsonConvert.SerializeObject(dataList);

            // Write the JSON data to the file
            System.IO.File.WriteAllText(fileName, jsonData);
        }
    }
}*/
